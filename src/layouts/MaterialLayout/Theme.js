import getMuiTheme from 'material-ui/styles/getMuiTheme';

const Theme = getMuiTheme({
  fontFamily: 'RobotoDraft, sans-serif',
  palette: {
    primary1Color: 'rgb(0, 121, 107)',
  }
});

export default Theme;
